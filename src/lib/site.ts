/**
 * Canonical origin for the site.
 *
 * Used as the `metadataBase` in the root layout so every relative
 * `alternates.canonical` resolves against the real production domain, and as
 * the base for the sitemap, robots.txt, and JSON-LD structured data.
 */
export const SITE_URL = "https://www.bhgforklifttraining.com";
